// Admin can also wash cars: this reuses the worker dispatch screen so the
// admin sees the same availability toggle + order queue + order alert sound.
export { default } from '@/screens/AvailabilityScreen';
